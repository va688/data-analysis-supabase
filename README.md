# data-analysis-supabase
An end-to-end data project integrating Supabase (PostgreSQL) and R to design, secure, and analyze a relational database. The project implements row-level security, role-based access control, and uses R (dplyr, ggplot2) to generate insights and visualizations from real data.
